# Set the description of the function
set_desc add "voila la description de la fonction generate2"

# Set the list of example
set_desc add "exemple pour la fonction generate2"

# Set the list of options with description
set_opt_list add -opt3 "premiere argument de generate2"
set_opt_list add -opt4 "deuxieme argument de generate2"

if [ "$_SET_DEFAULT_" = "1" ]; then
  # Set default values to the options.
  mopt3=# option : -opt3 "premiere argument de generate2"
  mopt4=# option : -opt4 "deuxieme argument de generate2"
fi
